/** Default disk-backed project loaded when the editor starts. */
export const DEFAULT_PROJECT_ID = "demo_robot";

/** Must match `PROJECT_ID_PATTERN` in `project_loader.py`. */
export const PROJECT_ID_PATTERN = /^[A-Za-z][A-Za-z0-9_-]{0,63}$/;
