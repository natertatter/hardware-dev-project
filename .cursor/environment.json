{
  "name": "EDA Platform",
  "install": "pip install --user -e \".[dev]\" && cd ui && npm ci",
  "terminals": [
    {
      "name": "api",
      "command": "python3 -m uvicorn eda_platform.api.main:app --host 0.0.0.0 --port 8000"
    },
    {
      "name": "ui",
      "command": "cd ui && NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev"
    }
  ],
  "ports": [
    { "name": "API", "port": 8000 },
    { "name": "UI", "port": 3000 }
  ]
}
